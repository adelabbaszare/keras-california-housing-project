# 🏠 پروژه تخمین قیمت مسکن کالیفرنیا با Keras

این پروژه یک **مینی‌پروژه** برای **پیش‌بینی قیمت مسکن در کالیفرنیا** با استفاده از یک **شبکه عصبی (Neural Network)** و کتابخانه **Keras** است.  
دیتاست مورد استفاده، مجموعه داده `california_housing` موجود در Keras می‌باشد.

---

## 🏁 هدف پروژه
هدف، **ساخت و آموزش یک مدل رگرسیون شبکه عصبی** است که مشخصات یک ملک (مانند موقعیت جغرافیایی، تعداد اتاق‌ها، جمعیت منطقه و...) را به عنوان ورودی دریافت کرده و **قیمت آن (Median House Value)** را به عنوان خروجی تخمین بزند.

---

## 💾 دیتاست

این پروژه از دیتاست `california_housing` که مستقیماً از `keras.datasets` لود می‌شود، استفاده می‌کند.  
اطلاعات بیشتر در مورد این دیتاست: **[Keras California Housing Dataset](https://keras.io/api/datasets/california_housing/)**

### ✳️ ویژگی‌ها (Features)
- **MedInc:** درآمد متوسط در بلاک  
- **HouseAge:** سن متوسط خانه‌ها در بلاک  
- **AveRooms:** متوسط تعداد اتاق‌ها  
- **AveBedrms:** متوسط تعداد اتاق‌های خواب  
- **Population:** جمعیت بلاک  
- **AveOccup:** متوسط تعداد ساکنین  
- **Latitude:** عرض جغرافیایی  
- **Longitude:** طول جغرافیایی  

### 🎯 هدف (Target)
- **MedHouseVal:** ارزش متوسط خانه (قیمت)

---

## 🛠️ نحوه راه‌اندازی و اجرا

برای اجرای این پروژه، مراحل زیر را دنبال کنید:

### ۱. شبیه‌سازی (Clone) یا دانلود پروژه
(اگر پروژه را در گیت‌هاب آپلود کرده‌اید، در غیر این صورت، فایل‌ها را در یک پوشه قرار دهید)

### ۲. ساخت و فعال‌سازی محیط مجازی (Virtual Environment)
توصیه می‌شود برای ایزوله کردن وابستگی‌ها، از یک محیط مجازی استفاده کنید.

```bash
# ساخت محیط مجازی
python -m venv venv

# فعال‌سازی در لینوکس/مک
source venv/bin/activate

### ۳. نصب وابستگی‌ها
فایل requirements.txt شامل تمام کتابخانه‌های مورد نیاز است.

```pip install -r requirements.txt```

۴. اجرای اسکریپت آموزش
اسکریپت اصلی پروژه train.py است. این اسکریپت مدل را بارگذاری، آموزش و ارزیابی می‌کند.

```python train.py```

### 📈 نتایج
پس از اجرای اسکریپت، نتایج ارزیابی مدل بر روی داده‌های تست نمایش داده می‌شود.
معمولاً معیارهایی مانند زیر گزارش می‌شوند:

MSE (خطای میانگین مربعات): ...

MAE (خطای میانگین مطلق): ...

# فعال‌سازی در ویندوز
.\venv\Scripts\activate
